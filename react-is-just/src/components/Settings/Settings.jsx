import React from 'react';
import "./Settings.css";

const Settings = () => {
    return (
        <div className="app-settings">
            Settings
        </div>
    );
};

export default Settings;